const Navbar = () => {
  return (
    <nav>
      <div className="d-flex justify-content-between">
        <div>{/* <Image src={} alt="logo" /> */}</div>
      </div>
    </nav>
  );
};

export default Navbar;
